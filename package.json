{
  "name": "ghost-particle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "npm run check-facts && npm run test:unit && npm run test:e2e",
    "check": "tsc --noEmit && npm run check-facts && npm run test:unit",
    "check-facts": "node scripts/check-facts.mjs",
    "test:unit": "node --test \"tests/unit/*.test.ts\"",
    "test:e2e": "playwright test",
    "shots": "node scripts/shots.mjs",
    "deploy": "npm run build && rsync -az --delete dist/ ghost-particle.exe.xyz:~/ghost-particle-dist/ && ssh ghost-particle.exe.xyz 'sudo rsync -a --delete ~/ghost-particle-dist/ /var/www/ghost-particle/'"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/three": "0.186.0",
    "typescript": "6.0.2",
    "vite": "8.3.0"
  },
  "dependencies": {
    "three": "0.186.0"
  }
}
